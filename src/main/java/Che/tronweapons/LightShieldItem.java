package Che.tronweapons;

import net.minecraft.world.item.ShieldItem;
import net.minecraft.world.item.Item;

/** Vanilla shield blocking, shield use animation, durability and projectile protection. */
public class LightShieldItem extends ShieldItem {
    public LightShieldItem(Item.Properties properties) {
        super(properties);
    }
}
